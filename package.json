{
  "name": "ai-quiz-hub",
  "version": "1.0.0",
  "description": "Daily AI quiz generator with Local, HuggingFace, OpenAI, and DeepSeek modes.",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
